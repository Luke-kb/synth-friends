<% if current_user && !Profile.exists?(user_id: current_user.id) %>
    <% controller.redirect_to new_profile_path %>
<% end %>

<div class="container">
    <div class="jumbotron text-center">
        <h1 class="display-4">Welcome to Synth Friends!</h1>
        <p class="lead">A Lending and Renting Marketplace for Synth and Audio gear</p>
        <hr class="my-4">
        <a class="btn btn-primary btn-lg" href="<%= listings_path %>" role="button">Browse Listings</a>
    </div>
    <%= render 'shared/displaylistings' %>
</div>